.DS_Store
*.bak
*.log

# Generated by scripts/generate-blog.js (cron, every 15 min) from the live
# blog API -- fully reconstructible, not source. sitemap.xml and blog.html's
# BLOG_POSTS region are also cron-managed but stay tracked since they're
# pre-existing files with only a portion regenerated.
/blog/
tmp-test-builds/
